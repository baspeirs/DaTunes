import React from "react";
import '../Styles/Header.css'

function Header() {
    return(
        <header>
            <h1>Da Tunes</h1>

            {/* eventually this will be split into two Tools Components [ logo and search bar ] */}
        </header>
    )
}

export default Header